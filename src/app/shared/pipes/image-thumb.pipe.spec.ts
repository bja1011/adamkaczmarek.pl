import { ImageThumbPipe } from './image-thumb.pipe';

describe('ImageThumbPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageThumbPipe();
    expect(pipe).toBeTruthy();
  });
});
